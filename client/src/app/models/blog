<!-- <div *ngIf="!newPost">

    <div *ngFor="let blog of blogPosts" class="card mb-3">
        <div class="flex_row">

            <img [src]="blog?.imagePath" [alt]="blog?.title" class="card-image">

            <h5 class="card-title">{{blog?.createdBy}}</h5>
        </div>

        <img [src]="blog?.imagePath" class="card-img-top" [alt]="blog?.title">
        <div class="card-body">
            <div *ngIf="email !== blog?.createdBy" (click)="likeBlog(blog._id)" class="icon">
                <span *ngFor="let liker of blog?.likedBy">

                    <p class="card-text" *ngIf="email == liker? like: !like">
                        <span *ngIf="like">
                            <mat-icon>favorite</mat-icon>
                        </span>
                        <span *ngIf="!like">
                            <mat-icon>favorite_border</mat-icon>
                        </span>
                    </p>
                    <!-- <p class="card-text" *ngIf="email == liker? !like: like">

                      
                    </p> -->
</span>

</div>
<p class="card-text">
    <strong>
        {{blog?.likes}}

    </strong>

</p>

<!-- <img [src]="blog?.imagePath" [alt]="blog?.title" class="card-image1"> -->
<h5 class="card-title">{{blog?.createdBy}}</h5>

<p class="card-text">{{blog?.body}}</p>
<p class="card-text"><small class="text-muted">{{blog?.createdAt | date: 'EEEE, dd MMM, yyyy'}}</small>
</p>
<a [routerLink]="['/edit-blog/', blog._id]" *ngIf="email === blog?.createdBy"><button type="button" name="button"
        class="btn btn-sm btn-success m-2 px-4">Edit</button> </a>
<a (click)="deleteBlog()" *ngIf="email === blog?.createdBy"><button type="button" name="button"
        class="btn btn-sm btn-danger m-2 px-4">Delete</button> </a>
<!-- [routerLink]="['/delete-blog/', blog._id]" -->
<p-confirmDialog [style]="{width: '50vw'}" [baseZIndex]="10000" rejectButtonStyleClass="p-button-text">
</p-confirmDialog>

</div>
</div>

</div> -->